apiVersion: infra.nephio.org/v1alpha1
kind: ClusterScaleProfile
metadata: # kpt-merge: /scale-profile
  name: scale-profile
  annotations:
    config.kubernetes.io/local-config: "true"
spec:
  autoscaling: false
  siteDensity: low
